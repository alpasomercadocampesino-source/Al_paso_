# Configuración de Cloud SQL (PostgreSQL / Supabase)
SQL_HOST=
SQL_USER=
SQL_PASSWORD=
SQL_DB_NAME=
SQL_ADMIN_USER=
SQL_ADMIN_PASSWORD=

# Configuración de variables de entorno para el envío opcional de correos por SMTP
SMTP_USER=alpasomercadocampesino@gmail.com
SMTP_PASSWORD=

# Nota: la configuración de Firebase Web SDK no se lee de variables de entorno,
# viene de firebase-applet-config.json (ver src/lib/firebase.ts).
